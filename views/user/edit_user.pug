extends ../layout

block content
    form(action="/user/" + user.id + '/edit',method="POST")
        input(type="text", name="nombre", placeholder="Introduce tu nombre...", value=user.nombre)
        button(type="submit") Guardar